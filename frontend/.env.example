# Usar API Express (por defecto activo). Pon false para volver a mocks + localStorage.
# VITE_USE_API=false

# Opcional: URL absoluta del backend (si no usas proxy de Vite en /api)
# VITE_API_URL=http://localhost:4000
