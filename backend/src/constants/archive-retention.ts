/** Días en papelera antes de eliminación automática (alineado con el frontend). */
export const ARCHIVE_RETENTION_DAYS = 30

export const ARCHIVE_RETENTION_INTERVAL = `${ARCHIVE_RETENTION_DAYS} days`
